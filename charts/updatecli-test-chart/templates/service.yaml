apiVersion: v1
kind: Service
metadata:
  name: {{ include "updatecli-test-chart.fullname" . }}
  labels:
    {{- include "updatecli-test-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "updatecli-test-chart.selectorLabels" . | nindent 4 }}
